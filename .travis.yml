# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

# Specify folder
install:
  - Rscript -e 'install.packages(c("devtools","roxygen2","testthat"));devtools::install_deps("project-4-L1-regularization/l1regularization")'
script:
  - Rscript -e 'devtools::check("project-4-L1-regularization/l1regularization/")'
